# EOD Report — Tuesday, July 7, 2026 (worldcup session)

Six PRs merged and deployed: #112–#118. Headline: **the Anthem Hub is COMPLETE —
53 tracks, all 48 teams + 5 FIFA songs.** Production: `worldcup-2026-sandy.vercel.app`.

---

## Shipped today

### PR #112 — news knockout/group leak (owner report: "Belgium demolishes USA in dominant Group D performance")
Same bug class as the match-page leak (#106) but in the AI news prompts: the fast-recap
path passed `GROUP:` unconditionally and literally instructed "what it means for Group X"
on every match. All three generators (fast recaps/previews, 6am backfill + day round-ups,
dashboard stories) are now stage-aware via `KNOCKOUT_START`/`classifyRound`; the standings
snapshot fed to the dashboard-stories model counts group games only. **Self-heal**: each
news generation run deletes knockout-dated stories naming a specific group
(`\bGroup [A-L]\b`) and regenerates them — the wrong Belgium recap fixed itself on the
next pinger tick.

### PRs #113–#116 — ANTHEM HUB COMPLETE 🏆 (53/53)
Owner uploaded 30 new songs across three batches; each batch was diffed against the
manifest **by Drive file ID** (dedup is inherent — no need to separate old/new in the
folder):
- #113: 18 teams + 1 FIFA + 2 audition copies (AUS/IRN had two versions; schema is
  one-anthem-per-team so the spares imported unlinked, playable side-by-side)
- #114: 8 more teams (AUT, CGO, CPV, CUR, CZE, GER, JOR, NOR); audition losers deleted
  per owner's picks ("The Socceroos" + Ghadamgari V2 won); "Winners Get To Toast"
  swapped to the re-uploaded copy instead of duplicating (byte-identical file detected)
- #115: final 3 (SUI, SWE, TUR) → **full 48-team coverage**
- #116: **Curaçao + Congo DR failed the first import — the DB uses api-football codes
  (CUR, CGO), not FIFA codes (CUW, COD)**. Manifest corrected with an authoring-time
  comment; also fixed teamMeta mapping CGO to Republic-of-Congo's flag (now `cd`).
- Verified live after each owner tap: 53 tracks, zero strays, zero dupes, audition
  copies pruned, only the TBD bracket placeholder shows "coming soon".

### Owner admin taps (all done)
Wipe+Reimport Anthems ×3 (chunked, session-auth button — the designed path),
Seed Full Squads (200), Seed Clubs (200). Leagues page now shows **412 players with
club data (up from 78)** — whether full squads (~1,248) landed or the 60s function
limit trimmed the loop is tomorrow's first check (chunk the route like batch-anthem).

### PR #117 — backlog §14: "The Soundtrack Survives" (owner-approved design)
Anthem × bracket, UX-first: ONE bracket with a music layer — ♪ on every card,
silenced/alive states per elimination, "Songs Still Standing: N" countdown strip,
hub Alive/Silenced badges, "Last Song Standing 🏆" share moment. A separate
music-only bracket was explicitly rejected (split navigation, reads as a poll).

### PR #118 — owner's evening batch (4 of 6 fixed same night)
1. **Bracket never stale** — was DB-only (lagged until the 2:30am sync); now overlays
   the live schedule feed: fresh scores win, TBD slots upgrade instantly, newly
   announced QF/SF fixtures appear before the DB ingests them.
2. **Standings clarity** — knockout-era subtitle ("Final group-stage tables"), gold
   "Group stage complete → View Knockout Bracket" banner, entropy strip relabeled
   "72/72 Group Matches · Done" so 72 never reads as a stale total (~100 of 104
   matches played; these tables only ever count the 72 group games).
3. **Predict page** — days newest-first (tonight's games on top), filter chips
   (All/Upcoming/Live/Finished/My Picks/Knockout/Groups), and a knockout "Group X"
   label leak fixed on prediction cards (round label now).
4. **Travel Pulse moves** — headline totals were full-tournament constants (+hardcoded
   1.5M/5.4B) so they literally could not change. Now progress-aware: revenue/visitors
   "to date" grow with each completed match, "Matches played 100/104", projections
   kept alongside and everything labeled modelled estimates.

### Also answered today
- **Admin panel guidance**: only 3 one-time taps were needed (Squads → Clubs → Ingest);
  Sync Fixtures + News Recaps are automated (nightly cron + 15-min pinger).
- **Gemini folder audit QC'd**: its 48-team count matched the manifest 1:1 — but a
  folder count can't catch deploy-level truth (the CUR/CGO code mismatch proved it).
- **SEED_SECRET appears ACTIVE** — the legacy hardcoded secret was refused by prod
  (401), which is the self-activating rotation working. Verify remaining checklist:
  CRON_SECRET, vercel.json cron `?secret=` params, GH `NEWS_REFRESH_SECRET`.

---

## Tomorrow's list (in order)

1. **Chunk `seed-full-squads`** (60s-limit class, same fix as batch-anthem) → exact
   player count to ~1,248; then re-tap + Ingest Player Stats backfill check.
2. **Drill-downs (owner #3/#4)**: Club & League Breakdown page — click league → clubs
   → players; The Officials page — click referee → full profile/history. Design for
   daylight quality, not midnight speed.
3. **"The Soundtrack Survives"** (§14) — with all 48 songs live.
4. **UX Phase 2** — hub landing pages (Now/Race/Fan Zone) + light-mode polish.
5. **Featured mashup metrics.**
6. **Security rotation verification** (SEED_SECRET confirmed; check the rest).
7. Truth-audit residue §13 (fetchedAt staleness stamps, weather cache age, seed rows).

## Watch items
- QFs start Thursday (FRA–MAR): first live test of Live Pressure, market ticks,
  stats-grounded commentary, "Scorer TBC" path, and stage-aware news.
- Travel Pulse: confirm the running totals tick after Thursday's games.
