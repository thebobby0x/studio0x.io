# Session Log — Thursday, July 16, 2026 (overnight continuation of 7/15)

## podiumSchedule DEPLOYED (PRs #149–#150)

Owner + session completed production setup overnight:
- Neon DB (direct connection string — the POOLED string fails `prisma db push`;
  first build error, swapped and documented in the README).
- Vercel project `podiumschedule` (root `podiumschedule/frontend`), env:
  DATABASE_URL (direct), SYNC_SECRET, CRON_SECRET.
- Gotchas hit and fixed:
  1. Initial import build skipped by our own ignoreCommand (latest main commit was
     docs-only) → README commit (PR #149) kicked the first real build.
  2. Pooled Neon URL failed db push → direct URL.
  3. "x Forbidden" on first sync → owner had copied the env var NAME (Vercel copies
     the name on click) + sensitive env values are write-only (Edit shows empty).
     Resolution: set a NEW SYNC_SECRET value, redeploy. Auth now trims secrets
     both sides (PR #150).
- **First Sync All Mapped succeeded** — WC26/UCL/Europa via TheSportsDB + F1 2026
  via Jolpica. The fixtures depot is real: /api/events queryable.

## Post-sync fixes + handoff

- Soccer syncs failed while F1 succeeded → TheSportsDB retired free key "3"; default
  now "123" (PR #152, deployed). Free tier caps season event lists — paid TSDB_KEY
  (~$9/mo) unlocks full seasons; flagged to owner.
- Owner shared a "migration admin info" Google Doc at end of night — NOT readable from
  this session (Drive connector needs interactive approval; direct fetch blocked by
  network policy). Owner to paste contents or approve the connector next session.
  Link kept in chat only (public repo — no dev/private URLs).

## Open (carried)

- 13 unmapped competitions (TSDB ids via /admin as owner finds them).
- podiumschedule.studio0x.io domain — whenever owner wants; stays unlinked from
  live products per the dev-access rule until declared market-ready.
- Nightly 03:00 cron will run its first auto-sync tonight.

## FULL END-OF-DAY REPORT (7/15 → 7/16 overnight, PRs #138–#153)

### Shipped & live
| Area | Outcome |
|---|---|
| Branding | footy26 → **podiumMetrics** under **sportOS**; family footer w/ quiet links |
| Launch | **podiummetrics.studio0x.io** live (GoDaddy CNAME + Vercel domain, canonical OG) |
| Auth | Sign-in on custom domain (AUTH_URL + Google redirect URI); super admin confirmed |
| Crons | CRON_SECRET set — nightly jobs authenticate for the first time since rotation |
| P0 fixes | Stale-score freshness (#139), TBD-downgrade guards (#145), round windows (#147) |
| New product | **podiumSchedule deployed & syncing** (own Vercel + Neon, /events depot API) |
| Docs | Module registry, portfolio report for BK's inventory, env table, gotchas #22–#23 |

### Decisions recorded (durable)
sportOS structure + three-name-layer module model; depot-per-domain architecture;
dev-access rule (no public in-dev URLs); flat subdomains; no internal renames this
iteration; post-WC26 rebrand/simplification list; QCC shorthand.

### Tomorrow / mobile-dispatch queue
1. **Watch tonight's crons** — first authenticated nightly runs (podiumMetrics 2:30am
   sync; podiumSchedule 3:00am). Verify dashboard freshness in the morning.
2. **3rd place FRA-ENG (Jul 18) + FINAL ESP-ARG (Jul 19)** — hardened pipeline's
   real test; watch hero labels + live scores.
3. TheSportsDB paid key decision (~$9/mo) → full season data in the depot.
4. Migration-admin Google Doc — paste contents or approve Drive connector.
5. Map remaining 13 competitions in podiumSchedule /admin (TSDB ids).
6. Roadmap tasks standing: TM screen, podiumSchedule cross-wiring + domain,
   landing-page link (store session), cross-tournament compare (§17),
   post-tournament extraction (#8).
