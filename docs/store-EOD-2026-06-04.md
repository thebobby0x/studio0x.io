# studio0x market — End-of-Day Report
**Date:** 2026-06-04 · **Owner:** Robert H. Kelley Jr. (b@studio0x.io)

A digital-asset marketplace that sells AI-generated kits (PDF + editable files),
with Stripe checkout, an AI creator pipeline, a super-admin console, a customer
portal with a downloads library, and a per-customer OAuth "connections + doers"
layer (Drive/Gmail/X). Built on a static front-end (GitHub Pages) + Supabase
(Postgres/Auth/Storage/Edge Functions) + Stripe + Claude.

---

## 1. Live URLs
- **Storefront:** https://studio0x.io/store/
- **Product page:** https://studio0x.io/store/product.html?slug=listingpilot-ai
- **Customer account:** https://studio0x.io/store/account.html
- **Super-admin:** https://studio0x.io/store/admin.html
- **Company site (unchanged):** https://studio0x.io

> Hosting: GitHub Pages serves `main` of `thebobby0x/studio0x.io` at `studio0x.io`; the store lives under `/store/`.

## 2. Today's snapshot (live data)
| Metric | Value |
|---|---|
| Live products | 5 |
| Draft products (AI, hidden) | 4 |
| Orders (all test mode) | 7 |
| Paid/fulfilled | 7 |
| Test revenue | **$613.00** (Stripe sandbox) |
| Customers | 1 (b@studio0x.io, test buys) |
| Entitlements (download grants) | 11 |
| Admins | 1 (superadmin) |
| AI agents | 3 |
| Customer connections | 0 (none activated yet) |

## 3. What was built & shipped today (PRs #1–#12, all merged to `main`)
1. **Storefront + Stripe** — landing pages w/ CTA, impulse add-ons / order bumps, server-trusted pricing, webhook fulfillment → entitlements → signed downloads.
2. **Super-admin console** — Overview KPIs, Products (CRUD + uploads + delete + per-product add-ons), Add-ons, Orders, Customers, AI agents, Team (invite/roles), Account (change password).
3. **AI creators** — `claude-opus-4-8`, prompt-cached. From a brief → metadata + full deliverable. Renders a **branded cover** (Syne/DM Mono via resvg), a **branded PDF**, and **editable DOCX + Markdown**. Products land as hidden drafts for review.
4. **Editable-files upsell (+$7)** — global order bump that grants the editable source files for every purchased product.
5. **Email delivery** — Resend-ready in the webhook (sends a branded "downloads ready" email; no-ops until `RESEND_API_KEY` is set).
6. **Cart + multi-item checkout** — localStorage cart, drawer, checkout fulfills multiple products.
7. **Real catalog** — 5 published AI products w/ covers (real estate, fitness, Shopify, local biz, newsletter); file-less demos retired.
8. **Customer portal** — login/signup, **My Downloads** (orders + always-fresh signed links), **Connections** hub, change password.
9. **Per-customer OAuth framework** — secure token storage (hidden from clients), PKCE, owner-only RLS.
10. **Doers** — `action-run`: **Save-to-Drive**, **Gmail draft**, **Post-to-X** (verify purchase → use stored token → act).

## 4. Infrastructure & connections
### Supabase — project `studio0x-market`
- **Ref / project_id:** `cmwdvxvxlfjeaknftobb` · **Org:** studio0x · **Region:** us-east-1
- **API URL:** https://cmwdvxvxlfjeaknftobb.supabase.co
- **Functions base:** https://cmwdvxvxlfjeaknftobb.functions.supabase.co
- **Storage buckets:** `product-assets` (private), `product-images` (public)
- **Migrations:** `0001_init` → `0005_customer_connections`
- **Edge functions deployed:** `create-checkout-session`, `stripe-webhook`, `get-order`, `ai-create-product`, `get-my-downloads`, `oauth-start`, `oauth-callback`, `action-run`, `admin-create-user`, `seed-sample-assets` (one-off, now disabled)

### Stripe
- Connected in **Sandbox / Test mode**.
- Webhook endpoint → `https://cmwdvxvxlfjeaknftobb.functions.supabase.co/stripe-webhook`, event `checkout.session.completed`.
- Test card `4242 4242 4242 4242`. **Not yet in live mode.**

### Anthropic (Claude)
- `ANTHROPIC_API_KEY` set as a Supabase secret (the originally-pasted key was **rotated** by you). Model `claude-opus-4-8`, prompt caching on.

### GitHub
- Repo `thebobby0x/studio0x.io`; all work merged to `main` via PRs #1–#12. Working branch `claude/eloquent-faraday-v3ceD`.

## 5. Secrets inventory (Supabase → Edge Function secrets)
**Set ✅:** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `ALLOWED_ORIGIN`, `ANTHROPIC_API_KEY` (+ auto: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`)
**Not set yet ⬜ (features dark until added):**
- `RESEND_API_KEY` (+ optional `FROM_EMAIL`) → purchase emails
- `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` → Drive + Gmail connection & doers
- `X_CLIENT_ID` / `X_CLIENT_SECRET` → X connection & post doer
- `META_CLIENT_ID/SECRET`, `LINKEDIN_CLIENT_ID/SECRET`, `TIKTOK_CLIENT_ID/SECRET` → those connections

## 6. Access / credentials
- **Super-admin:** `b@studio0x.io` — **temp password `Studio0x-cfbb1f21!` (CHANGE THIS).** Change in the admin **Account** tab.
- **OAuth redirect URI** to register in every provider app: `https://cmwdvxvxlfjeaknftobb.functions.supabase.co/oauth-callback`

## 7. Security notes
- Stripe prices are server-trusted (browser can't tamper).
- OAuth tokens stored server-side only; **hidden from clients via column grants**; owner-only RLS; CSRF state + PKCE.
- DB advisor warnings addressed (internal functions' public RPC revoked).
- **To do:** rotate the temp admin password; before go-live, run the Supabase **security advisor** again and switch Stripe to live keys.

## 8. Known gaps / not done yet
- Connections/doers are **plumbed but dark** until provider OAuth apps + secrets exist.
- Google Gmail/Drive scopes require **Google app verification** before non-test users can connect (add yourself as a test user to try immediately).
- **Phase 3 (AI social/funnels)** not started — tables scaffolded only.
- **singularityLab** connection is a placeholder (no public API yet).
- Custom domain not purchased/pointed (store runs under `studio0x.io/store/`).
- Purchase **emails** off until Resend key is set.

---

## 9. Roadmap
**Phase 3 — AI social-media experts + funnels** (next big build): social agent drafts multi-step funnels per product, generates platform-native posts + a schedule, posts via the customer/owner connections, monitors & optimizes. Tables `funnels`, `social_campaigns` already exist.

**Activation track (mostly your external setup):**
- Stripe **live mode** → real sales.
- **Resend** → purchase emails.
- **Google + X OAuth apps** → light up connections + doers.
- **singularityLab API** → wire as the #1 connection.
- **Custom domain** → repoint + `ALLOWED_ORIGIN`.

**Polish backlog:** customer "my downloads" search/filter; coupons (Stripe promo codes already enabled); abandoned-cart email; affiliate program; analytics dashboard; render even richer PDFs (charts/branded section dividers).

---

## 10. Plan for tomorrow morning
**Goal: light up one real connection and watch a doer work end-to-end.** Recommended order:

1. **(5 min) Rotate the admin password** — admin → Account tab.
2. **(15–20 min) Create the Google OAuth app** (I'll walk you through it):
   - Google Cloud → new project → enable **Drive API** + **Gmail API**.
   - OAuth consent screen (External), add yourself as a **Test user**.
   - Credentials → **OAuth client (Web)** → Authorized redirect URI = `https://cmwdvxvxlfjeaknftobb.functions.supabase.co/oauth-callback`.
   - Copy client id/secret → set `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` in Supabase secrets.
3. **(5 min) Test the loop:** account.html → Connections → Connect Google → consent → back to account → on a purchased kit click **Save to Google Drive** → confirm files land in your Drive. Then test **Draft in Gmail**.
4. **(15 min) X / Twitter app** (same pattern) → set `X_CLIENT_*` → test **Post to X**.
5. **Then choose:** kick off **Phase 3 (AI social/funnels)**, or switch **Stripe to live** + set **Resend** to take real orders with email delivery.

I can drive steps 2–4 with you click-by-click, and start Phase 3 as soon as you say go.
