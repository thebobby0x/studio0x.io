# Studio0x — Working Backlog (captured 2026-06-30)

Living to-do combining (A) things I know need doing from the codebase, and
(B) the strategic features the owner wants next. Ordered roughly by priority /
dependency. Check items off as they ship.

---

## 0. Immediate / housekeeping

- [ ] **Set `NEWS_REFRESH_SECRET` GitHub repo secret** = `wc2026studio0x` so the new
      `refresh-news.yml` Action (every 15 min) can keep news fresh. (Settings →
      Secrets and variables → Actions → New repository secret.)
- [ ] **Backfill news now:** `/admin` → "Generate News Recaps" to fill the Sun→now gap.
- [ ] **Re-seed bracket after July 3** — knockout (R32→Final) fixtures are TBD until
      api-football publishes them; re-run `/api/seed`.
- [ ] **Seed Full Squads + Ingest Player Stats** — `/admin` buttons; needed for PPI™
      and player-level metrics (player count currently low without it).

---

## 1. SECURITY (do before any real launch / white-label)

- [ ] **Admin secret is hardcoded in a PUBLIC repo.** `checkAuth` accepts the literal
      `"wc2026studio0x"`, which is in source + `vercel.json`. Anyone can call seed/
      purge/wipe admin endpoints. **Fix:** require `SEED_SECRET` env var only, drop the
      hardcoded fallback, rotate the secret, update `vercel.json` cron URLs + the GH
      Action secret. HIGH priority before launch.
- [ ] Audit all `/api/admin/*` and `/api/seed` routes for the same pattern.
- [ ] Consider moving destructive admin actions behind the authenticated SUPER_ADMIN
      session only (no query-secret) for the UI, keeping secret-auth for crons.

## 2. Platform / infra

- [ ] **Vercel Pro upgrade** — lifts the three Hobby limits that bit us 6/30: 1 GB Blob
      cap, 60 s function limit, 1 h log retention; also enables native sub-daily crons
      (could retire the GH Action). Trigger: adding the 2nd anthem page / scaling traffic.
- [ ] **Blob hygiene** — periodic "Free Up Blob Storage"; consider auto-eviction (TTL or
      size cap) for `tts/` + `deep-dives/` caches so they can't refill the quota.
- [ ] **Prisma migrations** — switch from `db push` to `prisma migrate` before launch so
      schema changes are versioned/reversible.
- [ ] **Custom domain** — `worldcup.studio0x.io` (DNS + Vercel domain settings).
- [ ] **Anthropic credit monitoring** — commentary/stories fail silently when depleted;
      add a balance check / alert.

## 3. Anthems (in-flight)

- [ ] **Toward 48 teams** — as each anthem is created, add `{ driveFileId, teamCode,
      title }` to `src/lib/anthemManifest.ts` (titles preserved verbatim), then Wipe +
      Reimport. Send me Drive IDs + exact titles.
- [ ] **"NON WC26 Anthems" page** — new `nonWcAnthemManifest.ts` + page + section; Costa
      Rica ("Pura Vida, mae") lives here. Independent reimport from the new Drive folder.

---

## 4. UI/UX overhaul ⭐ (owner priority #1)
**Goal:** beautiful, clean, simplified, not busy — a pleasure from first open.

- [ ] Design pass on the global shell: typographic hierarchy, spacing, fewer competing
      accents, calmer default density. Establish a tighter design system on top of the
      existing Tailwind tokens.
- [ ] Dashboard: lead with one clear hero moment (live match or "today"), progressively
      disclose the rest. Reduce simultaneous panels.
- [ ] Move proprietary-metric **formula footnotes off cards** → a `/methodology` page
      (keeps the mystique; declutters). (Already noted in ROADMAP §4.)
- [ ] Make Studio0x brand primary over "WC 2026" in nav/header (platform-first story).
- [ ] Mobile-first polish — most of tonight's testing was desktop; verify the phone view.
- [ ] Consistent empty/loading/error states across pages.

## 5. Featured Section — proprietary "mashup" metrics ⭐ (owner #2)
**Goal:** combine metrics not normally discussed together → signature insights.

- [ ] Define 3–5 flagship composite metrics (e.g. Clutch × Upset, Pressing × Transition
      Danger, Form × Elimination Proximity, Goal Gravity × Momentum). Name + brand each.
- [ ] Build a Featured page that showcases them with strong visual storytelling.
- [ ] Each metric: clear "what it means" + link to `/methodology`.

## 6. Featured Section — full data + enrichment ⭐ (owner #3)
- [ ] Update ALL data/metrics end-to-end; verify each metric has live inputs.
- [ ] **Add missing players' clubs + leagues** (Club WC Impact™ depends on this).
- [ ] **Club World Cup cross-reference** — seed a 2nd tournament's player stats with a
      `tournamentTag` discriminator; side-by-side player comparison (per ROADMAP §2).
- [ ] Reverse enrichment: club form → national-team profile (ROADMAP §3).

## 7. Imagery ⭐ (owner #4)
- [ ] Pull player headshots + World Cup action imagery tied to the metric being shown
      (e.g. the player driving a Goal Gravity spike). Source/licensing TBD (api-football
      has some media; confirm rights). Cache in Blob (watch quota → Pro).

## 8. Monetization ⭐ (owner #5)
- [ ] **AdMob / ad slots** — wire real ad content into the sponsor admin panel; Google
      AdSense fallback fill. Verify the existing sponsor section renders + tracks.
- [ ] **Sponsorship section** — confirm impression/click tracking is functioning.

## 9. White-label "Metrics Engine as a Service" ⭐ (owner #6)
**The business case: the stat engine is the product; WC is the reference deployment.**

- [ ] Design a clean public **API** over the proprietary metrics (auth via API key /
      `WHITE_LABEL` role). Versioned, documented, rate-limited.
- [ ] Separate the sport-agnostic core from WC-specific data (per "Platform Architecture
      Intent" in CLAUDE.md) so the feed is reskinnable.
- [ ] Partner-facing docs + a "For Partners / White Label" CTA for logged-out users.

## 10. Modular product composition ⭐ (owner #7)
**Goal:** interchangeable modules that combine into new products.**

- [ ] Define a module contract/boundary so products plug in cleanly.
- [ ] **PodiumSelect** — travel concierge (note: WC Travel Pulse already models team/fan
      travel — natural integration point).
- [ ] **SingularityLab** — AI Agent Engine "under the hood" (commentary/stories/Go-Deeper
      already use an AI layer — candidate to factor out as the shared engine).
- [ ] **eventOS / humanOS** — modular platforms; define how they slot in.
- [ ] Monorepo structure to host these as composable apps/packages.

---

## Reference
- Root-cause learnings + tooling from the 6/30 session: `docs/eod-2026-06-30.md`
- Deferred/strategic context: `docs/ROADMAP.md`, CLAUDE.md "Tomorrow's Priorities" + gotchas
