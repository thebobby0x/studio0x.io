# EOD Report — 2026-07-17 (evening session, ran into 7/18 early UTC)

**Session focus:** Final Weekend package · The Roundtable (owner's custom voice cast) ·
durable story archive · truth hard-rule · visual pass · studio0x.io feature banner.
Final weekend: 3rd place FRA–ENG Sat Jul 18 · **Final ESP–ARG Sun Jul 19**.

---

## Shipped (merged to main, all deployed)

| PR | What |
|---|---|
| #155 | Traffic-independent api-football budget (shared data cache) + ET/pens polish — full-distance final (ET + shootout) fits the 7,500/day plan with ~2.9k headroom |
| #156 | Final Weekend spotlight (time-boxed, auto-expires Jul 21) + homepage product cards for podiumMetrics & contentOS on studio0x.io |
| #157 | Final Weekend v2 — spotlight "Match not found" fix (fixture id, not DB cuid), HUGE screaming masthead, **The Roundtable** podcast panel, five pregame final features (goalkeeping/defense/legacy/lineups/tactics, Sonnet, grounded) |
| #158 | Metric explainer "i" popovers (15 metrics, no formulas — mystique preserved), team-color diverging goal graph (ARG blue above / ESP red below — sanctioned palette exception), influencer persona |
| #159 | Roundtable v2 — gapless audio (prefetch pipeline), expressive per-persona delivery, international panel |
| #160 | **The owner's custom ElevenLabs cast wired**: Ricky Riquelme (old Boca legend), Roberto Madrid (Real Madrid keeper, Catalan), Henry Futois (ex-PSG, French). Lorraine Footy is BRITISH (host). Server-side persona→voiceId mapping; env overrides ELEVENLABS_VOICE_* |
| #161 | Accent fix round 1 — panel to `eleven_multilingual_v2`, blob cache rev v2 |
| #162 | **Durable story archive** — editorial + final-feature stories persist to NewsStory (deterministic upsert ids); `RoundtableEpisode` model (ONE canonical episode per fixture, frozen at kickoff — also fixes per-instance episode divergence that defeated the shared TTS cache); `/api/stories/export` (JSON/Markdown, filters) for socials/blogs; export links on /news |
| #163 | Accent fix round 2 — `AUDIO_RESPELL` pronunciation lexicon (dale→[dah-leh], Henry→[awn-ree], vale, vamos, madre mía, écoutez…, names Riquelme/Futois), applied to spoken text only, all four Roundtable speakers; **CONTENT TRUTH hard rule codified in CLAUDE.md** |
| #164 | Panel personas send NO voice_settings — each custom voice uses the owner's stored VoiceLab settings |
| #165 | Accent fix round 3 — panel on **`eleven_v3`** with audio-only `[strong French/Spanish/Argentinian accent]` performance tags; auto-fallback to multilingual_v2 (without tag) if v3 errors. Cache rev v5 |
| #166 | (this PR) Stadium SVG art on the Final Weekend spotlight (showcase + champions variants), podiumMetrics feature banner on studio0x.io, this EOD + CLAUDE.md updates |

## The voice/accent saga — state of play

Owner reports: (a) accents lost/reduced, (b) native words English-pronounced, (c) after
round 1+2, incognito test = "some pronunciation better, accents don't come through well."

Stack now live (gotcha #24): `eleven_v3` + accent tags → fallback multilingual_v2 ·
VoiceLab stored settings (no overrides) · AUDIO_RESPELL lexicon · PERSONA_AUDIO_REV=v5.

**Morning checklist for BK:**
1. Test in a FRESH/incognito tab (open tabs replay old audio from page memory).
2. First play per line is slower (v3 regeneration) — that's the fix working.
3. If accents still weak: remaining lever is on the ElevenLabs side — **remix each voice
   in VoiceLab with stronger accent descriptors** ("thick Buenos Aires accent", "heavy
   Catalan accent, rolls every R"). Voice IDs stay the same; zero code changes.
4. If a specific word sounds wrong: send the word + target sound ([like-this]) — one-line
   lexicon patch.
5. If Vercel logs show `[TTS] eleven_v3 failed` → v3 not enabled for the account; audio
   continues on fallback. Tell Claude and we re-evaluate.

## Story archive & backfill (owner directive: facts only — HARD RULE)

- Everything now persists: news depot (already DB), editorial + final features
  (archive-on-generate), Roundtable episodes (canonical per fixture).
- **Backfill is one click:** `/admin` → "Generate News Recaps" — idempotent, walks all FT
  matches, writes missing game recaps + daily round-ups dated to the match day (~104
  games, ~$1 Haiku). THEN owner reviews timeline and flags landmark fixtures wanting
  richer Sonnet features.
- Export for socials/blogs: `/api/stories/export?format=md` (+ `category`/`team`/`from`/
  `to` filters). Links in /news header.
- CONTENT TRUTH hard rule recorded in CLAUDE.md (applies to every current & future
  surface; audit confirmed all AI prompts carry invent-bans).

## Images (owner request #3) — honest status

The session's network policy **blocks all image CDNs** (Unsplash/Pexels/Wikimedia →
CONNECT 403 at the gateway), so real photos could be neither downloaded nor verified.
Rather than hotlink unverifiable URLs (broken-image roulette on final weekend), shipped
**original SVG stadium-night art** (floodlights, bowl tiers, pitch geometry, confetti
champions variant) on the app spotlight + the studio0x.io feature banner — license-clean
by construction. **To add real photography:** BK drops image files (owned or
Unsplash-downloaded) into Drive or the repo, or enables a network policy that allows
image CDNs — wiring them in is minutes of work.

## studio0x.io landing page (store session — please skim)

Root `index.html` changes are ADDITIVE only, `pm-` class prefix, marked with HTML
comments: a time-boxed podiumMetrics feature banner (own navy/gold palette, stadium SVG,
real fixtures, CTA to podiummetrics.studio0x.io) inserted between the flagship block and
the products grid. Safe to trim after Jul 20. Product cards from #156 unchanged.

## Watch items for the final weekend

- 3rd place FRA–ENG kicks off Sat Jul 18 ~21:00Z — first live test of the hardened
  pipeline (max-merge, ET/pens statuses, quota-optimal polling).
- Roundtable episode for the final is DB-frozen at kickoff; audio regenerates once per
  line at rev v5 (first listeners pay ~2–4s/line, then global cache).
- Blob storage: TTS rev bumps orphan old audio files (v2–v4 keys). If quota nears 800MB,
  the nightly cron evicts; manual "Free Up Blob Storage" also purges tts/.

## Open items (carried)

- Owner morning actions: voice test (checklist above) · backfill click · optional
  VoiceLab remix · flag landmark fixtures for deep features.
- TheSportsDB paid key decision (podiumSchedule full seasons, ~$9/mo).
- podium family TM screen before permanent branding.
- Cross-session: store session to review the additive index.html banner.
