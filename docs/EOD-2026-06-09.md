# studio0x market — End-of-Day Report
**Date:** 2026-06-09 · **Owner:** Robert H. Kelley Jr. (b@studio0x.io)

Today was a **go-live day**: the redesign + real imagery shipped to production, the
store got its **own dedicated Stripe account** (cleanly separated from singularityLab),
a **7-day refund policy** went live, all prices moved to a **charm "ends-in-7" ladder**,
and a **launch-price countdown** is now ticking on every product page. Five PRs merged
(#18–#22). The only thing left before real revenue is **purchase testing**, which you'll
run in the AM.

---

## 1. Live URLs
- **Storefront:** https://studio0x.io/store/
- **Cheapest test product ($27):** https://studio0x.io/store/product.html?slug=shopcopy-ai-kit
- **Terms & refund policy (new):** https://studio0x.io/store/terms.html
- **Customer account:** https://studio0x.io/store/account.html
- **Super-admin:** https://studio0x.io/store/admin.html

## 2. What shipped today (PRs #18–#22, all merged & live)
1. **Redesign + 24 images live (PR #18).** Merged the storefront redesign with square
   1:1 tiles + the real product photos + the guarantee removal. The site at `/store/`
   now reflects all of it.
2. **Terms & 7-day refund policy (PR #19).** New `terms.html`: 7-day window, every request
   via a complete form (→ emails support), human review, "try the product first" in good
   faith, and a **statutory-rights safe-harbor** clause. Linked from every footer + a
   "By purchasing you agree to…" line at checkout. (Top-10 refund reasons + a 3-bucket
   triage rubric captured in the chat thread for the future eval system.)
3. **Dedicated Stripe account + shared-account safety (PRs #20, #21).**
   - Decision: studio0x market gets its **own Stripe account** (the **"studio0x"** account —
     EIN, profile, and Biz-account bank all confirmed), separate from singularityLab
     (which lives in the **STUDIO0X LLC** account).
   - The Supabase project (`cmwdvxvxlfjeaknftobb`, "studio0x-market") is **shared** with
     singularityLab, and edge-function secrets are **project-global** — so the store now
     reads **dedicated** secret names (see §4) to avoid collision.
   - Webhook guard: checkout stamps `metadata.source = "studio0x-market"`; the webhook
     ignores any event without it (defends against cross-brand event delivery).
4. **Pricing → "ends-in-7" ladder.** Applied store-wide in the DB:
   - Active tiers: **$27** (was $29) · **$37** (was $34) · **$47** (was $39).
   - Compare-at anchors set to a consistent ~half-off story: **$57 / $77 / $97**.
   - Add-ons: Editable **$7** · Lifetime **$17** · Commercial **$47** (your bump) · Done-For-You **$97**.
5. **Launch-price countdown — "Option A" (PR #22).** `config.js → launchPriceUntil`
   (set to **2026-06-16**) drives a live buy-box line: **"🚀 Launch price · Going up to
   $57 in N days — lock in the intro price."** Reframes compare-at as the *future* price
   for honest FOMO; auto-reverts to the normal strikethrough after the date.
6. **Two visual fixes (PR #22).** Product image container `16/11 → 1/1` (images are
   1080² — the old ratio cropped the bottom off); nav "Get it now →" CTA forced to dark
   ink so it's readable on the pink fill (it was inheriting `--muted` grey).

## 3. Edge functions — deployed today
All three redeployed to the live project, `verify_jwt=false` preserved:
- `create-checkout-session` → **v13** (source stamp + dedicated secret)
- `get-order` → **v11** (dedicated secret)
- `stripe-webhook` → **v15** (source guard + dedicated secrets)

## 4. Stripe / secrets state (important for future sessions)
- **Store's Stripe = the "studio0x" account** (live key wired). Live webhook created
  under **Your Account** → `checkout.session.completed` →
  `https://cmwdvxvxlfjeaknftobb.functions.supabase.co/stripe-webhook`.
- **Dedicated Supabase secrets (studio0x account):**
  - `STRIPE_SECRET_KEY_marketEdgeFunctions` ✅ set
  - `STRIPE_WEBHOOK_SECRET_marketEdgeFunctions` ✅ set
- `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` in the same project belong to
  **singularityLab** — left untouched.
- **Stripe account cleanup (pending, see §6):** the studio0x account has **2 stale full-access
  `sk_live` keys** (81 & 163 days idle) + **1 old restricted key** to delete once the new
  key is confirmed working. Two accidental empty accounts ("New business" / "New Business")
  to close.

## 5. HELD pending your go-ahead
- **Server-side auto-repricing.** The countdown is **display-only** right now. To make the
  *actual charge* rise automatically at T=0, I'll add date logic to `create-checkout-session`
  (charge each product's compare-at once the window closes). This raises the **whole catalog**
  to regular price (~2×) unless the date is reset — you wanted to okay that knowingly first.
  Resets are on me (you ping, I update the date). **Not enabled yet.**

## 6. Go-live checklist — status
**Done today:** ✅ redesign/images live (PR #18) · ✅ refund/terms page (PR #19) ·
✅ Stripe live key + live webhook wired (dedicated account) · ✅ shared-account webhook guard.

**Still required before/at the AM test:**
1. **Purchase test** — buy the $27 product end-to-end; confirm success page, download link,
   and the order in admin. *(You're doing this in the AM.)*
2. **`RESEND_API_KEY` (+ `FROM_EMAIL`)** — confirm it's set, or buyers get no delivery email
   (the webhook no-ops on email until it exists).
3. **Rotate the temp admin password** (still default).
4. **Enable leaked-password protection** (Supabase Auth — one toggle).
5. **Rotate the OpenAI key** (treated as exposed; update `OPENAI_API_KEY` in the env config).

**Stripe tidy-up (security):** delete the 2 stale `sk_live` keys + old restricted key; close
the 2 empty "New business" accounts (confirm empty first).

## 7. ⚠️ Flag — the other side of the shared Stripe account
singularityLab's **own** webhook will now receive studio0x's `checkout.session.completed`
events (and vice-versa). Our side is guarded; **singularityLab's webhook needs the same
kind of `source` guard** or studio0x sales could create junk orders / wrong-brand emails
there. Separate codebase — can't fix it from here.

## 8. Plan for tomorrow
1. **Purchase tests** (you) → I'll tail the webhook logs live and confirm fulfillment.
2. Decide on **server-side auto-charge** for launch pricing (§5).
3. Knock out the **security trio** (admin password, leaked-password protection, OpenAI key)
   + the **Stripe key/account cleanup** (§4, §6).
4. Optional polish: extend the launch badge to **store cards + cart**; add a live
   **H:M:S** countdown near the deadline for extra urgency.
5. **Bake the "ends-in-7" rule** into `ai-create-product` so future products auto-conform.
6. Resume the parked **naming taxonomy** + **eCom platform series** from the 06-06 report.

---
> Status: store **live** at `/store/` · prices **$27/$37/$47** (+7-ending add-ons) ·
> launch countdown ticking to **June 16** (display-only) · Stripe on its **own account**,
> secret + webhook wired · **purchase test is the last gate** before real revenue.
> No real orders yet. PRs #18–#22 merged.
