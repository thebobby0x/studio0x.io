{
  "buildCommand": "npx prisma db push --schema=prisma/schema.prisma && npx prisma generate --schema=prisma/schema.prisma && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
